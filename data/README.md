# Data Directory

Place the cleaned friction archive files here:

```text
data/train.csv
data/test.csv
```

The raw proprietary archive is not tracked by Git. The feature preparation script removes segment and sub-segment identifiers from model inputs to avoid shortcut learning.
